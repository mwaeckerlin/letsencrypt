{
  "name": "@mwaeckerlin/letsencrypt",
  "version": "2.0.0",
  "scripts": {
    "build": "docker compose build",
    "start": "docker compose up --build --force-recreate --remove-orphans",
    "stop": "docker compose down",
    "test": "npm run test:image && npm run test:e2e",
    "test:image": "npm run build && bash tests/image-contract.sh mwaeckerlin/letsencrypt",
    "test:e2e": "bash tests/run-e2e.sh"
  }
}
